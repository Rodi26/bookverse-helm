global:
  imageRegistry: "${DOCKER_REGISTRY}"

platform:
  version: "qa"

web:
  repository: "${DOCKER_REGISTRY}/${PROJECT_KEY}-web-docker-internal-local/web"
  tag: ""
  env:
    BOOKVERSE_ENV: "QA"
    INVENTORY_BASE_URL: "http://inventory"
    RECOMMENDATIONS_BASE_URL: "http://recommendations"
    CHECKOUT_BASE_URL: "http://checkout"
  ingress:
    enabled: true
    className: ""
    host: "web.qa.bookverse.local"
    annotations: {}
    tls:
      enabled: false
      secretName: ""

inventory:
  repository: "${DOCKER_REGISTRY}/${PROJECT_KEY}-inventory-docker-internal-local/inventory"
  tag: ""

recommendations:
  repository: "${DOCKER_REGISTRY}/${PROJECT_KEY}-recommendations-docker-internal-local/recommendations"
  tag: ""

checkout:
  repository: "${DOCKER_REGISTRY}/${PROJECT_KEY}-checkout-docker-internal-local/checkout"
  tag: ""

